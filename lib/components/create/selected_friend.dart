class SelectedFirend {
  final String id;
  final String imagePath;
  final String name;
  final double price;
  final bool isCustomed;

  SelectedFirend(this.id, this.imagePath, this.name, this.price,
      {this.isCustomed = false});
}
